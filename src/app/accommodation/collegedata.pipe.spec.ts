import { CollegedataPipe } from './collegedata.pipe';

describe('CollegedataPipe', () => {
  it('create an instance', () => {
    const pipe = new CollegedataPipe();
    expect(pipe).toBeTruthy();
  });
});
